import React from 'react';
import { Link } from 'react-router-dom';
import styled from "styled-components"; 

const Topbackground  = styled.div`
    width:100%;
    height:800px;
`;

function HeroSection({ brand, products }) {
    return (
          <Topbackground> 
 
         </Topbackground>
    )
}

export default HeroSection
